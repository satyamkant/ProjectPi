spring:
  application:
    name: SecurityMs
  config:
    import: consul://casalocal.com:8500/SecurityMs-Dev/application # Specifies that configuration should be loaded from Consul
  cloud:
    consul:
      discovery:
        hostname: localhost
      config:
        enabled: true  # Enable Consul configuration management