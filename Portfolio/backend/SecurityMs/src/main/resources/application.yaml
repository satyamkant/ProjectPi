spring:
  application:
    name: SecurityMs
  config:
    import: consul://consul:8500/config/SecurityMs
  cloud:
    consul:
      config:
        enabled: true
        format: YAML
      discovery:
        enabled: true